# Typing_Speed_test
A simple Python Typing Speed Test application that measures typing speed (Words Per Minute) and accuracy by comparing user input with randomly selected sentences. Built using Python's built-in `random` and `time` modules.
